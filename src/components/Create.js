

function Create() {
    const heading="create a blog"
    return(
        <div className="home" style={{backgroundColor: '#FFFFFF'}}>
            <h1>{heading}</h1>
        </div>
    )
}
export default Create;